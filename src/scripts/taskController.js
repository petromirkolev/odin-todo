import "../css/task.css";
import {
  renderTaskView,
  renderAddTaskView,
} from "../renderers/taskRenderer.js";

const taskController = {
  renderTaskView,
  renderAddTaskView,
};

export default taskController;
